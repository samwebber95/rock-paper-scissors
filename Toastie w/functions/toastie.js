// Make toasties with fillings

function makeToastie(filling, quantity) {
  // take in filling and quantity
  if (quantity === 1) {
    // if quantity is > 1 print out monsieurs
    console.log(quantity + " " + filling + "Monsieur coming up");
  } else {
    console.log(quantity + " " + filling + "Monsieurs coming up");
  }
}
